# v0.7.27 generated Romanian diacritics and responsiveness fix — no delivery

Milestone: v0.7.27-generated-romanian-diacritics-and-responsiveness-fix-no-delivery

## Policy

- NO_SHARE
- NO_DELIVERY
- NO_PUBLIC_RELEASE
- NO_BUILD
- NO_ZIP

## Base

Started from v0.7.26 blocked final main:

ed1dce5 test: record extracted package browser readiness blocker (#388)

## Reason

v0.7.26 extracted-package human/browser readiness was blocked because:

1. Generated Romanian course text showed corrupted or incorrect diacritics/OCR output.
2. The extracted-package browser session became very slow / unresponsive.
3. Generated course bottom navigation required visual review because it could become heavy/overlapping.

## Change

This milestone adds a display-layer HTML response guard in services/api/web_app.py:

- normalizes common Romanian OCR/encoding artifacts in generated HTML responses:
  - ǎ -> ă
  - Ǎ -> Ă
  - ˘a / a˘ -> ă
  - cedilla/comma artifacts for ș / ț
- injects bottom-navigation CSS once to prevent fixed/overlapping visual behavior.
- removes duplicate tester-flow bottom navigation blocks when generated HTML contains more than one.
- keeps the last bottom navigation block to preserve navigation access.
- does not rewrite OCR source files, course Markdown, user data, or package artifacts.

## Delivery decision

This is not a tester delivery milestone.

The v0.7.25 ZIP remains blocked until a new extracted-package human/browser validation passes in a later milestone.

## Required next validation

After merge, a separate extracted-package/source browser validation must confirm:

- Romanian generated course text is readable in browser.
- Diacritics are displayed correctly for common Romanian text.
- Generated course pages remain responsive.
- Bottom navigation no longer overlaps or duplicates visually.

## Final marker

VOILA_V0_7_27_GENERATED_ROMANIAN_DIACRITICS_AND_RESPONSIVENESS_FIX_NO_DELIVERY_CHECK=PASS
