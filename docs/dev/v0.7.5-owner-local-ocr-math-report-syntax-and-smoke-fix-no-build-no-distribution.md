# v0.7.5-owner-local-ocr-math-report-syntax-and-smoke-fix-no-build-no-distribution

## Scope

This owner-local milestone repairs the v0.7.4 final-main validation gap.

The previous v0.7.4 PR was merged, but the console output showed that final validation was not trustworthy because:

- `services/api/web_app.py` failed Python syntax compilation with an `IndentationError`
- the v0.7.4 smoke import failed
- the v0.7.4 check continued after native command failures
- the v0.7.4 check script hit a broken fixture variable path

This milestone repairs that state by:

- import-move repairing owner-local OCR Math report fragments from `web_app.py`
- inserting a clean syntactically valid read-only implementation at a safe top-level FastAPI/import anchor with import relocation
- preserving the v0.7.1 summary/raw routes
- preserving the v0.7.2 polished card labels
- preserving the v0.7.3 read-only viewer route
- replacing the broken v0.7.4 smoke check with a delegation wrapper to the repaired v0.7.5 check
- adding this v0.7.5 repair check with real smoke validation

## Policy

This milestone is syntax and smoke-validation repair only.

It does not:

- edit reports
- accept suggestions
- reject suggestions
- auto-correct formulas
- add Formula OCR
- rewrite OCR text
- rewrite pages
- rewrite course content
- change Study
- change Progress
- build packages
- create ZIP files
- deliver files
- distribute files

## Validation

Run:

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/dev/check-v0.7.5-owner-local-ocr-math-report-syntax-and-smoke-fix-no-build-no-distribution.ps1
```

Expected policy: no build, no ZIP, no delivery, no distribution.