# v0.7.18 owner-local generate entrypoint and write smoke final freeze — no build / no distribution

## Baseline

This freeze starts after the protected-main merge and validation of:

```text
v0.7.17d-owner-local-controlled-fixture-write-smoke-no-build-no-distribution
FINAL_MAIN_HEAD=1ad23a1
VOILA_V0_7_17D_OWNER_LOCAL_CONTROLLED_FIXTURE_WRITE_SMOKE_CHECK=PASS
```

## Purpose

This milestone freezes the owner-local `/generate` recovery chain after the controlled write smoke passed on protected main.

It is a documentation/static-check milestone only.

## What was proven

### v0.7.17a — diagnostic

The `/generate` route failed with:

```text
NameError: name 'generate_for_pdf' is not defined
file=services/api/web_app.py
route=/generate
```

### v0.7.17b2 — stack isolation

The stack isolation reproduced the same `/generate` failure with a synthetic fixture and no delete/reset/cleanup contamination.

### v0.7.17c — fix

The missing `generate_for_pdf(pdf_path)` entrypoint was restored in `services/api/web_app.py`.

The entrypoint explicitly runs the existing local pipeline scripts:

```text
pdf_extract.py
outline_builder.py
normalize_outline.py
course_generator.py
course_polisher.py
```

### v0.7.17d — controlled fixture write smoke

The controlled synthetic fixture smoke passed on protected main:

```text
POST /upload -> 303
POST /generate -> 303
GET /?generated=<fixture>.pdf -> 200
expected_outputs_present=true
forbidden_requests_detected=false
generate_500_detected=false
```

Expected generated outputs were verified:

```text
pages.json
pages.md
course_outline.json
course_outline.md
course_outline.normalized.json
course_outline.normalized.md
course.md
course.cleaned.md
glossary.json
quiz.json
flashcards.json
ocr_corrections.json
```

## Policy

Allowed in this milestone:

- documentation update
- static source/documentation checks
- Python compile check for `services/api/web_app.py`

Forbidden in this milestone:

- no runtime server start
- no upload
- no generate
- no runtime write smoke
- no private/confidential PDF
- no delete
- no reset
- no cleanup
- no build
- no ZIP
- no delivery
- no distribution
- no feature changes
- no behavior changes
- no public UI expansion

## Static checks

The check script verifies that:

- `services/api/web_app.py` contains the v0.7.17c entrypoint marker
- `generate_for_pdf(pdf_path)` is defined
- the existing `/generate` route calls `generate_for_pdf(pdf_path)`
- the entrypoint references the real pipeline scripts
- v0.7.17b2, v0.7.17c, and v0.7.17d docs/check scripts exist
- v0.7.17b2, v0.7.17c, and v0.7.17d validation markers are present
- `services/api/web_app.py` compiles

## Validation marker

```text
VOILA_V0_7_18_GENERATE_ENTRYPOINT_AND_WRITE_SMOKE_FINAL_FREEZE_CHECK=PASS
```

## Final frozen state

```text
generate_entrypoint_restored=true
controlled_fixture_write_smoke_passed=true
runtime_write_performed_in_this_milestone=false
build_created=false
zip_created=false
delivery_performed=false
distribution_performed=false
real_private_confidential_data_used=false
delete_reset_cleanup_performed=false
feature_changes=false
behavior_changes=false
public_ui_expansion=false
```

## Recommended next step

Stop here, or open a separate future milestone only if a new owner-approved product decision is needed.
