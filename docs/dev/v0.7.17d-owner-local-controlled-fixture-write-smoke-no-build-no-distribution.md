# v0.7.17d owner-local controlled fixture write smoke — no build / no distribution

## Baseline

Previous merged fix milestone:

```text
v0.7.17c-owner-local-generate-entrypoint-fix-no-build-no-distribution
FINAL_MAIN_HEAD=104a381
VOILA_V0_7_17C_OWNER_LOCAL_GENERATE_ENTRYPOINT_FIX_CHECK=PASS
```

## Purpose

This milestone retries the original controlled fixture write smoke after the missing `/generate` entrypoint was restored.

The smoke uses a synthetic local PDF fixture only. It performs exactly one controlled upload and one controlled generate request through local HTTP requests, then verifies that expected owner-local generated outputs exist.

## Policy

Allowed:

- owner-local server start
- synthetic fixture PDF outside the repository
- one automated `POST /upload` for the synthetic fixture
- one automated `POST /generate` for the uploaded synthetic fixture
- read-only GET checks
- local Uvicorn log capture
- local output existence checks under `data/output/<fixture-stem>`

Forbidden:

- no build
- no ZIP
- no delivery
- no distribution
- no public release
- no private/confidential PDF
- no delete
- no reset
- no cleanup
- no export
- no unrelated UI actions
- no feature changes
- no behavior changes
- no public UI expansion

## Expected generated artifacts

The synthetic fixture generate smoke should create or refresh local owner-only outputs such as:

```text
pages.json
pages.md
course_outline.json
course_outline.md
course_outline.normalized.json
course_outline.normalized.md
course.md
course.cleaned.md
glossary.json
quiz.json
flashcards.json
ocr_corrections.json
```

These are runtime data outputs and must not be committed.

## Evidence location

The check script writes evidence outside the repository:

```text
%TEMP%\voila-v0.7.17d-controlled-fixture-write-smoke
```

Expected evidence files:

```text
voila-v0.7.17d-controlled-fixture.pdf
uvicorn.stdout.log
uvicorn.stderr.log
controlled-fixture-write-smoke-summary.txt
```

## Validation marker

```text
VOILA_V0_7_17D_OWNER_LOCAL_CONTROLLED_FIXTURE_WRITE_SMOKE_CHECK=PASS
```

This marker means the controlled fixture write smoke passed. It does not create a build, ZIP, delivery, or distribution.

## Expected final status

```text
controlled_fixture_write_smoke_completed=true
upload_status=303
generate_status=303
generated_homepage_status=200
expected_outputs_present=true
delete_reset_cleanup_performed=false
build_created=false
zip_created=false
delivery_performed=false
distribution_performed=false
real_private_confidential_data_used=false
feature_changes=false
behavior_changes=false
public_ui_expansion=false
```

## Next step

After this milestone is merged and validated on protected main, the project can return to a read-only consolidation checkpoint or stop.
