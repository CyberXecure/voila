# v0.7.20 — controlled tester ZIP preparation, no delivery

## Status

Milestone explicit separat după v0.7.19b.

Baseline aprobat:

- v0.7.19 owner-local full function local smoke: PASS
- v0.7.19b owner-local route real smoke before ZIP: PASS
- v0.7.19b fixed OCR Math report lookup by adding a fast path for `data/output/{course_id}` before recursive scan

## Scope

Acest milestone pregătește local un ZIP candidat pentru testeri.

Output local așteptat:

```text
D:\dev\release-assets\voila\v0.7.20-controlled-tester-zip-preparation-no-delivery\
```

Artefacte așteptate:

```text
voila-v0.7.20-controlled-tester-windows-package-candidate.zip
voila-v0.7.20-controlled-tester-windows-package-candidate.zip.sha256
ZIP-PREPARATION-MANIFEST.json
ZIP-PREPARATION-SUMMARY.txt
NO-DELIVERY-NOTICE.txt
```

## Politică

Permis:

- pregătire locală ZIP candidat
- creare SHA256
- creare manifest local
- creare staging local sub `D:\dev\release-assets\voila\...`
- includere launchere batch în staging
- rulare gate v0.7.19b înainte de ZIP

Interzis:

- delivery către testeri
- distribuție
- OneDrive share
- GitHub release
- publicare
- installer
- includere date reale/private/confidențiale
- includere `data/input` sau `data/output` populate
- includere `.git` sau `.release-cache`

## Script

```text
scripts/dev/check-controlled-tester-zip-preparation-no-delivery.ps1
```

## Comandă locală de validare

În timpul dezvoltării milestone-ului:

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev\check-controlled-tester-zip-preparation-no-delivery.ps1 -AllowDirtyWorktree
```

Pe protected `main`, după merge:

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev\check-controlled-tester-zip-preparation-no-delivery.ps1
```

## Marker final

```text
VOILA_V0_7_20_CONTROLLED_TESTER_ZIP_PREPARATION_NO_DELIVERY_CHECK=PASS
```

## Non-goals

Acest milestone nu livrează ZIP-ul.

După PASS, delivery/distribution către testeri trebuie să fie un milestone separat aprobat explicit.
