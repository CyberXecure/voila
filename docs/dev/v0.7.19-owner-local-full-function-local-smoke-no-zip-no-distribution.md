# v0.7.19 — owner-local full function local smoke, no ZIP, no distribution

## Status

Milestone nou, explicit separat după v0.7.18.

Baseline confirmat înainte de acest milestone:

- protected `main` curat și up to date
- ultimul protected-main HEAD confirmat: `b5e846e`
- v0.7.18 final freeze/consolidare statică PASS
- marker final anterior: `VOILA_V0_7_18_GENERATE_ENTRYPOINT_AND_WRITE_SMOKE_FINAL_FREEZE_CHECK=PASS`

Lanț final închis înainte de v0.7.19:

- v0.7.17a: diagnostic `/generate` 500
- v0.7.17b2: stack isolation curat
- v0.7.17c: fix `generate_for_pdf` entrypoint
- v0.7.17d: controlled fixture write smoke PASS
- v0.7.18: final freeze/consolidare statică PASS

## Scope

Acest milestone adaugă un check local owner-only pentru a valida că funcționalitățile principale sunt prezente și pot fi fum-testate local înainte de orice ZIP pentru testeri.

Funcționalități acoperite:

- Upload
- Generate
- Course view
- Study
- Progress
- Exam Prep
- OCR Review / corrected OCR
- OCR Math report / OCR Math viewer
- Quick tools
- rute owner-local relevante

## Politică

Permis:

- verificări locale owner-local
- pornire locală Voila prin scripturile existente
- `GET` smoke pentru pagini locale
- verificare inventar rute/funții în `services/api/web_app.py`
- rularea check-urilor locale existente dacă sunt prezente
- scrieri locale temporare doar dacă sunt produse de aplicația locală sau de check-urile delegate existente

Interzis în acest milestone:

- build
- ZIP
- installer
- OneDrive share
- GitHub release
- delivery către testeri
- distribuție publică sau privată

## Fișier script

```text
scripts/dev/check-owner-local-full-function-local-smoke-no-zip-no-distribution.ps1
```

## Comandă locală de validare

Din repo:

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev\check-owner-local-full-function-local-smoke-no-zip-no-distribution.ps1
```

În timpul dezvoltării milestone-ului, înainte de commit, se poate rula cu worktree dirty permis:

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev\check-owner-local-full-function-local-smoke-no-zip-no-distribution.ps1 -AllowDirtyWorktree
```

## Marker final

Milestone-ul este acceptat doar dacă pe protected `main`, după merge, check-ul afișează:

```text
VOILA_V0_7_19_OWNER_LOCAL_FULL_FUNCTION_LOCAL_SMOKE_CHECK=PASS
```

## Non-goals

Acest milestone nu pregătește ZIP și nu livrează nimic testerilor.

ZIP-ul pentru testeri trebuie să fie un milestone separat, aprobat explicit, după PASS pe protected `main` pentru v0.7.19.

Milestone recomandat după PASS:

```text
v0.7.20-controlled-tester-zip-preparation-no-delivery
```

Distribuția/delivery către testeri trebuie să rămână un milestone separat după pregătirea ZIP-ului.
