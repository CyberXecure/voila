# v0.7.28 source/browser validation — BLOCKED / FAIL

Milestone: v0.7.28-source-browser-validation-blocked-no-delivery

## Final marker

VOILA_V0_7_28_SOURCE_BROWSER_VALIDATION_BLOCKED_NO_DELIVERY=FAIL

## Policy

- NO_SHARE
- NO_DELIVERY
- NO_PUBLIC_RELEASE
- NO_BUILD
- NO_ZIP

## Base

Started from v0.7.27 final main:

630e472 fix: polish generated Romanian display and navigation responsiveness (#389)

## Evidence files outside repo

Blocker JSON:

D:\dev\tester-runs\voila-v0.7.28-source-browser-validation-no-delivery\blocker-evidence\V0.7.28-SOURCE-BROWSER-VALIDATION-BLOCKED.json

Blocker MD:

D:\dev\tester-runs\voila-v0.7.28-source-browser-validation-no-delivery\blocker-evidence\V0.7.28-SOURCE-BROWSER-VALIDATION-BLOCKED.md

Raw script marker hits:

D:\dev\tester-runs\voila-v0.7.28-source-browser-validation-no-delivery\blocker-evidence\raw-script-marker-hits.json

## Human/browser blocker observations

The v0.7.28 source/browser validation could not be marked PASS.

Owner-observed blockers:

1. OCR Review / Correct OCR page displays raw JavaScript text in the browser.
2. OCR Math link/action does not open reliably.
3. Course navigation remains visually duplicated/overlapping/heavy.
4. The v0.7.27 display-layer fix is therefore not accepted as browser-validated.

Note: the capture script later showed HTTP probe failures because the source app was already stopped/refusing connections. The blocking evidence is the manual browser observation captured before stopping.

## Decision

This milestone is BLOCKED.

Do not rebuild ZIP.

Do not deliver this package to testers.

No OneDrive share update is allowed.

No GitHub release is allowed.

No public release is allowed.

## Required next milestone

A separate root-cause fix milestone is required before any package rebuild or tester delivery, focused on:

- removing raw JavaScript rendering from OCR Review / OCR Math pages
- making navigation server-side safe or otherwise correctly injected
- preventing duplicate/overlapping navigation bars
- fixing OCR Math navigation target behavior
- validating the fix in a real browser
