# v0.7.6 owner-local OCR Math report viewer regression guard

## Baseline

- baseline: v0.7.5-owner-local-ocr-math-report-syntax-and-smoke-fix-no-build-no-distribution
- protected main final HEAD: fd41900
- previous status: merged to protected main
- policy inherited from v0.7.5: no build, no ZIP, no delivery, no distribution

## Goal

Add a dedicated local regression guard for the owner-local OCR Math report viewer introduced and stabilized through v0.7.3-v0.7.5.

This milestone is validation-only. It does not add product behavior and does not modify the OCR Math report viewer implementation unless a future separate milestone explicitly allows that.

## Scope

This milestone adds:

1. A dedicated local check script:
   - `scripts/dev/check-owner-local-ocr-math-report-viewer-regression-guard-no-build-no-distribution.ps1`

2. This milestone document:
   - `docs/dev/v0.7.6-owner-local-ocr-math-report-viewer-regression-guard-no-build-no-distribution.md`

The check validates:

- `services/api/web_app.py` exists.
- Python compile for `services/api/web_app.py` passes.
- Owner-local OCR Math report viewer route exists:
  - `/owner/ocr-math-report/{course_id}/view`
- Viewer summary copy remains present:
  - `Sugestii detectate`
  - `Linii posibil afectate`
  - `Diagnostic local · read-only`
- Raw Markdown report reference/link remains present:
  - `ocr_math_report.md`
- Viewer rendering still has safe escaping in place.
- Only this milestone doc and dedicated check script are changed.

## Non-goals

This milestone intentionally does not touch OCR/pages/course/Study/Progress.

Specifically, it does not:

- change OCR generation;
- change page extraction or page rendering;
- rewrite `course.md` or `course.cleaned.md`;
- change Study mode;
- change Progress;
- change Exam Prep;
- change public UI;
- add cloud/API/LLM requirements;
- add delivery packaging;
- create a build;
- create a ZIP;
- publish or distribute anything.

## Policy

Required policy for this milestone:

```text
no build
no ZIP
no delivery
no distribution
owner-local only
read-only validation only
no OCR/pages/course/Study/Progress rewrite
```

## Expected PASS fields

```text
VOILA_V0_7_6_OWNER_LOCAL_OCR_MATH_REPORT_VIEWER_REGRESSION_GUARD=PASS
no_build=true
no_zip=true
no_delivery=true
no_distribution=true
ocr_pages_course_study_progress_rewrite_touched=false
viewer_route_present=true
viewer_read_only=true
raw_markdown_link_preserved=true
summary_metrics_present=true
final_status_check=PASS
```

## Final status

The milestone is complete only after:

1. The dedicated check passes locally.
2. `git diff --check` passes.
3. Only the dedicated check script and this documentation file are committed.
4. The PR is merged to protected main.
5. The dedicated check passes again on final main.