# v0.7.17a owner-local `/generate` 500 diagnostic — no build / no distribution

## Baseline

Previous successful milestone:

```text
v0.7.16-owner-local-manual-browser-read-only-smoke-no-build-no-distribution
FINAL_MAIN_HEAD=8703810
VOILA_V0_7_16_OWNER_LOCAL_MANUAL_BROWSER_READ_ONLY_SMOKE_CHECK=PASS
```

## Trigger

The attempted v0.7.17 controlled fixture write smoke failed locally.

Observed browser result:

```text
route=/generate
visible_error=Internal Server Error
v0.7.17_controlled_fixture_write_smoke=FAIL
```

The failed v0.7.17 files were removed and the repository was returned to a clean `main` state at `8703810`.

## Purpose

This milestone captures a controlled local diagnostic for the `/generate` 500 failure using only a synthetic fixture PDF.

This milestone does not claim that controlled fixture write smoke is fixed. It only records and captures diagnostic evidence so that a later fix milestone can be scoped safely.

## Diagnostic script note

The first diagnostic attempt exposed a script bug caused by using the reserved PowerShell variable name `$PID`. The fixed diagnostic script avoids reserved variable names and uses `$owningProcessId` instead.

## Policy

Allowed:

- owner-local server start
- local synthetic fixture PDF outside the repository
- one manual reproduction attempt of `/generate`
- local log capture under `%TEMP%`
- `GET /health`
- homepage/read-only checks
- browser open for local diagnostic

Forbidden:

- no build
- no ZIP
- no delivery
- no distribution
- no public release
- no private/confidential PDF
- no unrelated upload/generate attempts
- no delete/reset/cleanup
- no feature changes
- no behavior changes
- no public UI expansion
- no PR/merge claiming v0.7.17 PASS

## Evidence location

The diagnostic script writes local-only evidence outside the repository:

```text
%TEMP%\voila-v0.7.17a-generate-500-diagnostic
```

Expected evidence files include:

```text
voila-v0.7.17a-controlled-fixture.pdf
uvicorn.stdout.log
uvicorn.stderr.log
diagnostic-summary.txt
```

## Validation marker

The diagnostic script must end with:

```text
VOILA_V0_7_17A_OWNER_LOCAL_GENERATE_500_DIAGNOSTIC_CHECK=PASS
```

This marker means diagnostic capture completed. It does not mean `/generate` passed.

## Expected final status

```text
diagnostic_completed=true
generate_500_diagnostic_completed=true
build_created=false
zip_created=false
delivery_performed=false
distribution_performed=false
real_private_confidential_data_used=false
delete_reset_cleanup_performed=false
feature_changes=false
behavior_changes=false
public_ui_expansion=false
```

## Next step

After this diagnostic is reviewed, the next safe milestone may be:

```text
v0.7.17b-owner-local-generate-500-fix-no-build-no-distribution
```
