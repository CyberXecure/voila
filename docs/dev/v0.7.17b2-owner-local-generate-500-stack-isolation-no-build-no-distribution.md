# v0.7.17b2 owner-local `/generate` 500 stack isolation — no build / no distribution

## Baseline

Previous merged diagnostic milestone:

```text
v0.7.17a-owner-local-generate-500-diagnostic-no-build-no-distribution
FINAL_MAIN_HEAD=579cba8
VOILA_V0_7_17A_OWNER_LOCAL_GENERATE_500_DIAGNOSTIC_CHECK=PASS
```

## Why this milestone exists

The first v0.7.17b fix attempt failed and was cleaned without commit.

The attempted validation became contaminated by forbidden `POST /delete-from-library` requests, including a stale missing file error. Therefore a narrower diagnostic is needed before another fix attempt.

The first v0.7.17b2 diagnostic script attempt also failed before stack isolation because it used `$matches`, which collides with PowerShell automatic `$Matches`. The fixed script uses `$matchLineIndexes`.

## Purpose

This milestone isolates the stack trace for one controlled `POST /generate` attempt using a synthetic fixture PDF only.

This milestone does not apply a source fix.

## Policy

Allowed:

- owner-local server start
- captured local Uvicorn logs
- local synthetic fixture PDF outside the repository
- one manual upload of that exact fixture
- one manual generate attempt for that exact fixture
- `GET /health`
- homepage/read-only checks
- source context extraction from `services/api/web_app.py` for local evidence

Forbidden:

- no source fix
- no build
- no ZIP
- no delivery
- no distribution
- no public release
- no private/confidential PDF
- no delete
- no reset
- no cleanup
- no export
- no unrelated UI actions
- no feature changes
- no behavior changes
- no public UI expansion

## Evidence location

The check script writes local-only evidence outside the repository:

```text
%TEMP%\voila-v0.7.17b2-generate-500-stack-isolation
```

Expected evidence files:

```text
voila-v0.7.17b2-controlled-fixture.pdf
uvicorn.stdout.log
uvicorn.stderr.log
source-context.txt
stack-isolation-summary.txt
```

## Validation marker

The diagnostic script must end with:

```text
VOILA_V0_7_17B2_OWNER_LOCAL_GENERATE_500_STACK_ISOLATION_CHECK=PASS
```

This marker means stack isolation completed. It does not mean `/generate` is fixed.

## Expected final status

```text
stack_isolation_completed=true
generate_attempt_count=1
delete_reset_cleanup_performed=false
source_fix_applied=false
build_created=false
zip_created=false
delivery_performed=false
distribution_performed=false
real_private_confidential_data_used=false
feature_changes=false
behavior_changes=false
public_ui_expansion=false
```

## Next step

After reviewing the isolated stack and source context, the next milestone may be a narrower fix milestone:

```text
v0.7.17c-owner-local-generate-entrypoint-fix-no-build-no-distribution
```
