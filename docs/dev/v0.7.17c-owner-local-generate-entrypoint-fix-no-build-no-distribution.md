# v0.7.17c owner-local generate entrypoint fix — no build / no distribution

## Baseline

Previous merged diagnostic milestone:

```text
v0.7.17b2-owner-local-generate-500-stack-isolation-no-build-no-distribution
FINAL_MAIN_HEAD=d5acfd0
VOILA_V0_7_17B2_OWNER_LOCAL_GENERATE_500_STACK_ISOLATION_CHECK=PASS
```

## Confirmed root cause

The clean stack isolation confirmed:

```text
NameError: name 'generate_for_pdf' is not defined
file=services/api/web_app.py
line=1983
route=/generate
call=generate_for_pdf(pdf_path)
```

The isolated flow was:

```text
POST /upload -> 303
GET /?uploaded=<synthetic fixture>.pdf -> 200
POST /generate -> 500 Internal Server Error
```

No delete/reset/cleanup contaminated the diagnostic.

## Real pipeline context

The project already has concrete pipeline scripts:

```text
pdf_extract.py          -> pages.json/pages.md
outline_builder.py     -> course_outline.json/course_outline.md
normalize_outline.py   -> course_outline.normalized.json/course_outline.normalized.md
course_generator.py    -> course.md/glossary.json/quiz.json/flashcards.json
course_polisher.py     -> course.cleaned.md/ocr_corrections.json
```

## Purpose

This milestone restores only the missing `generate_for_pdf(pdf_path)` entrypoint used by the existing `/generate` route.

The fix explicitly runs the existing local scripts above in order. It does not add a generic dynamic shim and does not expand public UI.

## Policy

Allowed:

- source fix in `services/api/web_app.py` limited to restoring `generate_for_pdf`
- owner-local server start
- synthetic fixture PDF outside the repository
- one manual upload/generate validation using the synthetic fixture only
- local log capture under `%TEMP%`
- GET `/health`
- homepage/read-only checks

Forbidden:

- no build
- no ZIP
- no delivery
- no distribution
- no public release
- no private/confidential PDF
- no delete
- no reset
- no cleanup
- no unrelated UI actions
- no feature changes beyond restoring the existing `/generate` entrypoint
- no public UI expansion

## Validation marker

The check script must end with:

```text
VOILA_V0_7_17C_OWNER_LOCAL_GENERATE_ENTRYPOINT_FIX_CHECK=PASS
```

This marker means the controlled synthetic fixture no longer reproduces the missing-entrypoint `/generate` 500 failure.

## Expected final status

```text
generate_entrypoint_restored=true
generate_for_pdf_defined=true
controlled_fixture_upload_generate_manual=pass
build_created=false
zip_created=false
delivery_performed=false
distribution_performed=false
real_private_confidential_data_used=false
delete_reset_cleanup_performed=false
feature_changes=false
behavior_changes=limited_to_restoring_existing_generate_entrypoint
public_ui_expansion=false
```

## Next step

After this fix is merged and validated on protected main, retry the controlled fixture write smoke as:

```text
v0.7.17d-owner-local-controlled-fixture-write-smoke-no-build-no-distribution
```
