# Voila Windows Package Complete Runtime Source Copy Matrix

Milestone:

```text
v0.3.49-voila-windows-package-complete-runtime-source-copy-map-plan
```

## API/backend

```text
source: services/api/web_app.py
target: services/api/web_app.py
required: yes
reason: main Voila API entrypoint
```

```text
source: services/api/crop_editor_app.py
target: services/api/crop_editor_app.py
required: conditional
reason: separate crop editor on port 8790; include if current app links to it
```

```text
source: services/api/*.py
target: services/api/*.py
required: yes, after import review
reason: web_app.py may import local modules
```

```text
source: services/api/requirements.txt
target: services/api/requirements.txt
required: yes if present
reason: dependency inventory
```

## Python runtime

```text
source: .venv/
target: .venv/
required: recommended initial strategy
reason: fastest complete Windows tester runtime path
```

Alternative later:

```text
source: embedded Python build output
target: runtime/python/
required: future hardening option
reason: cleaner packaged runtime, less dev-environment coupling
```

## Frontend/static

```text
source: existing templates/static/assets used by services/api/web_app.py
target: same relative path under runtime source
required: conditional
reason: required only if web_app.py references files at runtime
```

## LanguageTool/Java

```text
source: known Java runtime folder
target: runtime/java/
required: conditional
reason: required if LanguageTool is bundled
```

```text
source: known LanguageTool folder or server jar
target: runtime/languagetool/
required: conditional
reason: required if LanguageTool is bundled
```

## Tesseract/OCR

```text
source: known Tesseract runtime folder
target: runtime/tesseract/
required: conditional
reason: required if OCR is bundled
```

```text
source: known tessdata folder
target: runtime/tessdata/
required: conditional
reason: required if OCR is bundled
```

## Package files generated during packaging

```text
source: generated by create-windows-package-launchers.ps1
target: START-VOILA.bat, STOP-VOILA.bat, scripts/, runtime/state/, runtime/logs/
required: yes
```

```text
source: copied by copy-package-legal-files.ps1
target: legal/
required: yes
```
