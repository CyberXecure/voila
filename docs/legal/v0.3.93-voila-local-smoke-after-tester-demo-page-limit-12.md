# v0.3.93 — Voila local smoke after tester demo page limit 12

## Result

PASS local.

## Summary

This milestone performs local smoke validation after the limited tester demo PDF page limit was increased to 12 pages in v0.3.92 / PR #206.

## Smoke result

PASS:

- Python compile:
  - `services/api/web_app.py`
  - `services/api/i18n.py`
  - `scripts/dev/smoke-limited-tester-demo.py`
- Romanian language-pack JSON validation.
- English language-pack JSON validation.
- Limited tester demo smoke.
- Direct limit/i18n check:
  - `VOILA_LIMITED_TESTER_DEMO is True`
  - `VOILA_TESTER_DEMO_MAX_PAGES == 12`
  - 13-page rejection message contains `12 pages per PDF`
  - 13-page rejection message contains `This PDF has 13 pages`
  - Romanian notice contains `maximum 12 pagini per PDF`
  - English notice contains `maximum 12 pages per PDF`
- Old 5-page tester limit references absent from source/docs, excluding generated/binary files.
- Local STOP before smoke completed.
- Local START completed.
- HTTP smoke:
  - `/health` returned 200 with `{"status":"ok"}`
  - `/` returned 200
  - `/quick-tools` returned 200
- Home page contains 12-page tester notice.
- Local STOP after smoke completed.
- Port 8787 free after STOP.
- Port 8081 free after STOP.

## Notes

The first UI text check attempted to assign the HTTP response to `$home`, which conflicts with PowerShell's read-only `$HOME` variable. This was a smoke-script variable-name issue only, not an application issue. The check was rerun with `$homeResponse` and passed.

## Scope

Local smoke/documentation only.
No application code changes.
No OCR engine changes.
No PDF processing logic changes.
No Study question generation changes.
No BKT algorithm changes.
No spaced-review scheduling changes.
No Review UI changes.
No Course Tools UI changes.
No package rebuild.
No ZIP creation.
No START/STOP package smoke.
No installer creation.
No GitHub release upload/publication.
No GitHub visibility change.
No payment/licensing implementation.
No final legal guarantee.

## Deferred

Known separate follow-ups:

- Rebuild Windows ZIP candidate with the 12-page tester demo limit.
- ZIP START/STOP smoke after rebuild.
- Release/tester asset staging refresh.
- Stable manual tester share package refresh.
- OneDrive tester share refresh.
