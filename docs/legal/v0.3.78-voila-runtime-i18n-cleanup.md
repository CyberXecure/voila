# v0.3.78 — Voila runtime i18n cleanup

## Result

PASS local.

## Summary

This milestone performs a safe runtime i18n cleanup after the Study/BKT/Progress stack.

## Implemented changes

- Removed duplicate Romanian runtime key for `recommended_because`.
- Added missing English runtime keys that were previously only covered by UI fallbacks:
  - recommended_because
  - bkt_diagnostics
  - recent_misses
  - last_question_type
  - last_correct
  - last_incorrect
  - question_type_stats
  - next_question_type
  - not_available
- Preserved existing effective Romanian and English UI behavior.
- Kept the legacy `TRANSLATIONS_EXT` block in place because a safety check showed that removing it would still change effective runtime values.

## Safety check result

The cleanup script refused to remove `TRANSLATIONS_EXT` because critical RO/EN values would change, including:

- ro.review
- ro.concept_status
- en.review
- en.total_questions

Therefore this milestone intentionally performs a safe partial cleanup only.

## Smoke result

PASS:

- Python compile check
- Runtime translation smoke for Romanian
- Runtime translation smoke for English
- RO `recommended_because`: Recomandat deoarece
- EN `recommended_because`: Recommended because
- RO BKT diagnostics labels present
- EN BKT diagnostics labels present
- Consolidat / Aproape consolidat / Nivel general preserved
- No intended UI behavior change

## Scope

Runtime i18n cleanup only.
No OCR engine changes.
No PDF processing logic changes.
No Study question generation changes.
No BKT algorithm changes.
No UI feature changes.
No ZIP rebuild.
No release publication.
No licensing/payment/GitHub visibility changes.

## Deferred

Known separate follow-ups:

- Normalize or remove `TRANSLATIONS_EXT` only after migrating remaining effective values into the base runtime dictionaries.
- Clean remaining rough duplicate `library` entries if confirmed safe.
- Add broader automated i18n regression tests.
