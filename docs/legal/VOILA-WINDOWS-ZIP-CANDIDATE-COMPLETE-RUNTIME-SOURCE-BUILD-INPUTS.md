# Voila Windows ZIP Candidate Complete Runtime Source Build Inputs

Milestone:

```text
v0.3.54-voila-windows-zip-candidate-complete-runtime-source-build-plan
```

## Required repository inputs

```text
scripts/release/create-complete-windows-runtime-source.ps1
scripts/release/build-windows-zip-candidate.ps1
scripts/release/copy-package-legal-files.ps1
scripts/release/validate-package-staging.ps1
services/api/web_app.py
services/api/crop_editor_app.py
services/api/requirements.txt
.venv/Scripts/python.exe
```

## Required generated runtime source

Generated by:

```powershell
.\scripts\release\create-complete-windows-runtime-source.ps1 `
  -RuntimeSourceRoot <runtime-source-root> `
  -PythonStrategy PackageVenv `
  -IncludeCropEditor `
  -LanguageToolStrategy Deferred `
  -OcrStrategy Deferred `
  -Force
```

Expected runtime source:

```text
<runtime-source-root>\voila
```

## Required build output root

Recommended temporary output root:

```text
<temp>\voila-v0.3.54-complete-runtime-source-zip-candidate-output
```

## Required version and release type

```text
Version: v0.3.54
ReleaseType: PublicBeta
```

## Required SHA256 verification

The future build milestone must verify that the generated SHA256 file matches the ZIP candidate.

## Required non-goals

The future build milestone must not:

```text
run START-VOILA.bat
run STOP-VOILA.bat
create EXE installer
create MSI installer
publish GitHub release
upload release assets
change GitHub visibility
add payment/licensing implementation
```
